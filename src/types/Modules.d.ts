/**
 * This is just a workaround for typescript to not throw an error when importing a module that doesn't exist.
 * This line should be in respective node modules.
 */
declare module 'react-native-razorpay';
